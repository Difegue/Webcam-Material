
#Uses Video::Capture::V4L to grab an image from the camera if the last one is too old (or doesn't exist at all)

#Returns the image data afterwards, essentially making this script a kind of mjpeg service.
